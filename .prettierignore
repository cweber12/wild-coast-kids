# Generated or vendored - formatting these would churn diffs for no reader.
.next/
out/
build/
coverage/
node_modules/
package-lock.json
next-env.d.ts

# Regenerated by `next dev` on every run; reformatting it fights the generator.
AGENTS.md

# Captured upstream payloads. These are evidence: what NOAA actually served, byte
# for byte, on the date named in the filename. Reformatting one would make the
# fixture a record of our formatter instead of a record of the endpoint, and the
# parser tests read them as raw text precisely so drift in the real bytes fails.
src/lib/__fixtures__/
